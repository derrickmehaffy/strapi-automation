# {{ ansible_managed }}
127.0.0.1	localhost
# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

{{ hostname_base_ip|default(ansible_default_ipv4.address) }} {{ (hostname_hostname ~ ( hostname_domain|ternary('.' ~ hostname_domain, '')))|default(ansible_fqdn) }} {{  hostname_hostname|default(ansible_hostname) }} {{ hostname_comment|default('') }}

{% for item in hostname_hosts|default([]) %}
{%   if item.hosts is not string and (item.hosts is mapping or item.hosts is sequence)  %}
{{ item.ip }}    {{ item.hosts | join(" ") }}
{%   else %}
{{ item.ip }}    {{ item.hosts }}
{%   endif %}
{% endfor %}
