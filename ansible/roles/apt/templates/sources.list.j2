# {{ ansible_managed }}
# {{ ansible_lsb.id }} {{ ansible_lsb.major_release }} {{ ansible_lsb.codename }} {{ item.comment|default('repositories')  }}

{% for repo in item.repositories|default([]) %}
{%   if repo.enabled %}
# {{ repo.comment|default('') }}
deb {{ repo.url }} {{ repo.section|default(ansible_lsb.codename) }} {{ repo.packages|join(' ') }}
{%     if repo.src_enabled %}
deb-src {{ repo.url }} {{ repo.section|default(ansible_lsb.codename) }} {{ repo.packages|join(' ') }}
{%     endif %}

{%   endif %}
{% endfor %}
